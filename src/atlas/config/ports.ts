/** Fixed runtime ports — Studio and App run independently. */
export const APP_PORT = 8081;
export const ATLAS_STUDIO_PORT = 8083;
export const ATLAS_DEV_API_PORT = 8084;

export const APP_URL = `http://localhost:${APP_PORT}`;
export const ATLAS_STUDIO_URL = `http://localhost:${ATLAS_STUDIO_PORT}`;
export const ATLAS_DEV_API = `http://127.0.0.1:${ATLAS_DEV_API_PORT}`;
