// Phase 5.9 ("Complete Presentation Reset") repoints this at `AtlasSpace.tsx`, same as `/atlas`
// — this route's URL is unchanged, only the component behind it.
export { default } from "@/atlas/studio/room/AtlasSpace";
